# Routine

Two-person accountability tracker. Paste a markdown plan, check things off, keep a streak together.

Static React app on GitHub Pages, Supabase for auth and data. See `routine-spec.md` for the full spec.

```
cp .env.example .env.local   # fill in the Supabase URL + anon key
npm install
npm run dev
```
